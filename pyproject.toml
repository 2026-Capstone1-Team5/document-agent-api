[project]
name = "document-agent-api"
version = "0.1.0"
description = "Document Agent API built with FastAPI"
requires-python = ">=3.11"
dependencies = [
    "fastapi[standard]>=0.135.1",
    "uvicorn[standard]>=0.41.0",
]
